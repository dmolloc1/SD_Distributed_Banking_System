server:
  port: 8080

spring:
  application:
    name: api-gateway-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
